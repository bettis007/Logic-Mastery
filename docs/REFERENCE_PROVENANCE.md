# Numerical reference provenance

The public reference is an AST-preserving extraction of numeric model, generator,
metric and policy routines. Documentary fixture code and source-file manifests
were omitted before any public commit. This is not byte-identical to the private
frozen source. Tests compare numerical labels and probabilities with a numeric
reference; no claim of full private-corpus validation follows from this tree.

The example inputs and labels are synthetic. No original source attachments,
private source narratives, credentials or datasets are included.
